# Generated Models

Runtime GLB files must come from `public/models/optimized/`.

The initial scaffold reserves `tiny_test_asset` as the first reviewable Meshy pipeline test, but no live Meshy API call has been made and no generated model is committed yet.
